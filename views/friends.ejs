<% include ./partials/header %>
<style>
    .jumbotron{
        background: #2C8FFE;
        height:100%;
        width:100%;
    }
</style>


    <div class="jumbotron">
        <div class="container">
            <br>
            <div class="row">
                
                <div class="col-md-2">
                    <br><div style="padding:12px;"></div>
                     <img style="height:170px;width:170px;border-radius:50%;" src="<%= user.image %>"/>
                 </div>
                 <div class="col-md-10">
                     <br><br>
                     <h1 style="font-size:100px;font-weight:800;color:white;"><%= user.username %></h1>
                     <% if (currentUser && user._id.equals(currentUser.id)) { %>
                     <button class="btn btn-warning"><a style="color:white;text-decoration:none;" href="/users/<%= currentUser._id %>"><i class="fas fa-cog"></i> Settings</a></button>
                     <% } else{ %><% } %>
                 </div>
             </div>
             <br>
             <div class="row">
                 <!--<% if (currentUser && user._id.equals(currentUser.id)) { %>
                        
                        <button class="button3-md"><a href="/users/<%= currentUser._id %>" style="color:white;">Edit Profile <i class="fa fa-pencil" aria-hidden="true"></i></a></button>
                        <form style="display: inline-block;" action="/user/<%= currentUser._id %>?_method=DELETE" method="POST">
                            <button class="button5-md" id="delete">Delete Profile <i class="fa fa-trash-o" aria-hidden="true"></i></button>
                        </form>
                <% } else{ %><% } %>-->
            </div>
        </div>
    </div>
<div class="container">
   <h1 style="font-size:45px;font-weight:700;">Acheivements 🏆</h1>
   <hr>
   <h2 style="font-size:16px;font-weight:400;color:#B2B2B2;">This feature is currently under development.</h2>
<br>
</div>

<br><br><br><br><br><br><br><br>
<% include ./partials/footer %>